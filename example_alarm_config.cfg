-- Edit this example file suit your needs and rename it to alarm_config.cfg
-- Then place it in your dzVents scripts folder together with alarm.lua

local _C = {}

_C.ALARM_TEST_MODE = false -- if ALARM_TEST_MODE is set to true it will prevent audible alarm

_C.ALARM_ZONES = {
	{
		name='Beach House', -- Short name for the alarm zone
		armingModeTextDevID=550, -- Domoticz virtual text device idx that You created for this zone. Holds the zones arming mode.
		statusTextDevID=554, -- Domoticz virtual text device idx that You created for this zone. Holds the zones event status.
		entryDelay=15, -- Entry delay in seconds
		exitDelay=20, -- Exit delay in seconds
		alertDevices={'Big Horn', 'Garden Horn'}, -- Named Domoticz devices that shall be switched on during an alert situation
		armAwayToggleBtn='Entrance Door Secret Switch', -- Optional switch device to toggle alarm status between Disarmed and Armed away
		armAwayTogglesNeeded=3, -- How many times the switch should be toggled within one minutes to activate
		armHomeToggleBtn='Bedroom Button', -- Optional switch device to toggle alarm status between Disarmed and Armed home
		armHomeTogglesNeeded=1, -- How many times the switch should be toggled within one minutes to activate
		mainZone=true -- You should have only one mainZone
	},
	{
		name='Garden Shed',
		armingModeTextDevID=551,
		statusTextDevID=555,
		entryDelay=0,
		exitDelay=0,
		alertDevices={'Garden Shed Siren', 'Another Siren', 'Big Horn', 'Garden Horn'},
		armAwayToggleBtn='Green Switch',
		armAwayTogglesNeeded=1,
		armHomeToggleBtn='',
		armHomeTogglesNeeded=1,
		mainZone=false
	},
}

return _C
